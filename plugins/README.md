# Plugins

This folder is reserved for future helper plugins. v0.3 reads `plugin.json` manifests and lists them in Tools > List plugins.

Plugin execution is intentionally not enabled yet, because executing arbitrary plugin binaries/scripts needs a permission and sandbox model.
